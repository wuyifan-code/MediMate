# Java从C盘迁移到D盘计划

## 当前状态
- Java已安装在C盘：`C:\Program Files\Microsoft\jdk-17.0.17.10-hotspot`
- 已下载最新Java安装包：`C:\Users\weixi\AppData\Local\Temp\microsoft-jdk-17.0.17-windows-x64.msi`

## 迁移步骤

### 1. 运行安装程序，指定D盘安装路径
- 使用PowerShell运行安装程序
- 指定安装路径为`D:\Program Files\Microsoft\jdk-17.0.17.10-hotspot`
- 使用静默安装模式，避免不必要的交互

### 2. 配置环境变量
- 更新JAVA_HOME环境变量指向新的D盘路径
- 更新PATH环境变量，将新的Java bin目录添加到最前面
- 确保旧的C盘Java路径不再存在于PATH中

### 3. 验证安装
- 执行`java -version`验证Java版本
- 执行`javac -version`验证JDK安装
- 检查环境变量配置

### 4. 测试项目构建
- 重新构建Android项目
- 验证Java编译错误是否解决

## 预期结果

Java将成功迁移到D盘，环境变量正确配置，Android项目能够正常构建，不再出现Java编译初始化错误。